
1、有的浏览器的汉字URI编码采用的UTF-8，有的采用的是GBK，需要前段界面在获取用户输入的时候采用两种编码，不然后台程序无法转换
.in2文件保存UTF-8编码的关键字，in保存GB2312编码的关键字

2、webkey.pat是用来匹配数据包，判断用户是否在进行搜索。

3、该脚本程序会用到iptables的string模块和layer7 模块和od命令（busybox里）模块。

4、config问价设置是否进行日志记录。